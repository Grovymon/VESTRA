# Vestra Sheets

`vestra-sheets.exe` is a separate spreadsheet application with a ribbon, multiple sheets,
CSV persistence and basic editing/formatting. XLS/XLSX/ODS formula and file compatibility is
provided only when the LibreOfficeKit bridge is completed and enabled.

