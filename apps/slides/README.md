# Vestra Slides

`vestra-slides.exe` is a separate presentation application with thumbnails, an editable canvas,
basic objects, reordering, a full-screen show and PDF export. It stores editable local
`.vestra-slides` files. PPT/PPTX/ODP compatibility requires LibreOfficeKit.

